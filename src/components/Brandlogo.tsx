import { Globe2Icon } from 'lucide-react';

const Brandlogo = () => {
  return (
    <span className="flex items-center gap-2 font-semibold flex-shrin-0  text-lg">
      <Globe2Icon className="size-8" />
      <span>SAAS App</span>
    </span>
  );
};

export default Brandlogo;
